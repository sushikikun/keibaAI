# Gate 2-F B3 Winner Residual Correction

This directory contains the Gate 2-F screening contracts and audit artifacts.

The earlier target-host attempt remains recorded in `portable_screening_result_v1/` as a resource-blocked historical run. A later higher-memory execution completed all eight WR-S / WR-H Fold computations. Its runner stopped only during post-fit CSV reporting; the source outputs were preserved and the reporting layer was reconstructed without another model fit. Full race-level validation passed, and the sanitized result is in `portable_screening_result_v2/`.

Both WR-S and WR-H are Screening-supported. WR-H is recommended for a separately authorized Confirmation freeze, but no formal Champion is selected. Confirmation and Final-lock access remain zero, and automation remains paused.
