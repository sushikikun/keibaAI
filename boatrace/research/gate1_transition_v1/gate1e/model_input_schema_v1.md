# Model input schema v1

The primary key is race_key; entry rows use race_key plus boat_number. Safe-core 49 order is inherited exactly from Gate 1-B feature_manifest. F1 and F2 use the fixed order in model_input_schema_v1.json. Target, auxiliary, audit, F3, and F5 fields are excluded from model input.
